import React from "react"; 
import { View, Text } from "react-native";

const SearchBar = () => {
    return (
        <View>
            <Text>empresas</Text>
        </View>
    );
};

export default SearchBar;
