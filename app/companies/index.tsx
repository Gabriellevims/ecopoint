import React from "react"; 
import { View, Text } from "react-native";

const index = () => {
    return (
        <View>
            <Text>empresas</Text>
        </View>
    );
};

export default index;
