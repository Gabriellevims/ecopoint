import React from "react"; 
import { View, Text } from "react-native";

const reuse = () => {
    return (
        <View>
            <Text>empresas</Text>
        </View>
    );
};

export default reuse;
